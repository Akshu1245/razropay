# Repository provenance

This repository contains the verified MandateGuard Policy Lab submission. The repository slug is `razropay`; the original placeholder README from the repository was `# razropay` and contained no implementation files. The exact submission archive and its independent verification record are preserved under `release/`.

The source remains a deterministic offline benchmark with a local provider simulator. It does not claim live Razorpay API execution or live payment movement.
